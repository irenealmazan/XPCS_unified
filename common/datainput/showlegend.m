% Note that a LEGEND variable is hidden in datainput, which merely lists
% the # scannumbers
% This just uses the normal legend(LEGEND) command to put it on the plot

disp(['Note: legend can be moved by mouse'])
legend(LEGEND)